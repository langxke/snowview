# 雪象 - 记录功能实施方案

## 📋 文档信息

**功能名称**：记录模块（日记/周记/随笔 + Markdown支持）  
**文档版本**：v1.0  
**创建日期**：2025年10月8日  
**预计工期**：4-5天  

---

## 1. 功能概述

### 核心价值
- 📝 **完善时间管理闭环**：计划 → 执行 → 反思记录
- 🤖 **AI内容保存**：保存AI对话中的有价值内容
- 📊 **数据沉淀**：形成个人知识库和成长档案
- 🔗 **深度集成**：关联任务、专注数据，形成完整上下文

### 核心功能
- 📝 支持 Markdown 格式编辑和渲染
- 📅 三种记录类型：日记、周记、随笔
- 🏷️ 标签系统（分类和筛选）
- 🔗 关联任务和专注数据
- 🔍 全文搜索
- 📌 置顶重要记录

---

## 2. 数据模型设计

### 2.1 JournalEntryHive（记录条目）
**TypeId**: 7

```dart
@HiveType(typeId: 7)
class JournalEntryHive extends HiveObject {
  @HiveField(0) String id;                    // 唯一标识
  @HiveField(1) String title;                 // 标题（可选）
  @HiveField(2) String content;               // Markdown 内容
  @HiveField(3) String type;                  // diary/weekly/note
  @HiveField(4) DateTime createdAt;           // 创建时间
  @HiveField(5) DateTime updatedAt;           // 更新时间
  @HiveField(6) List<String> tags;            // 标签列表
  @HiveField(7) String? mood;                 // 心情emoji（可选）
  @HiveField(8) List<String> relatedTaskIds;  // 关联任务ID
  @HiveField(9) int? dailyFocusMinutes;       // 当日专注时长（自动）
  @HiveField(10) bool isPinned;               // 是否置顶
  
  // 计算属性
  String get displayTitle;                    // 显示标题（无标题时用日期）
  String get dateFormatted;                   // 格式化日期
  int get wordCount;                          // 字数统计
  
  JournalEntryHive copyWith({...});
}
```

**Hive Box**: `journal_entries`

### 2.2 TypeId 分配

| TypeId | 类名 | 说明 |
|--------|------|------|
| 0-6 | ... | 已使用 |
| **7** | **JournalEntryHive** | **记录条目（新增）** |

---

## 3. Repository 层

### JournalRepository

```dart
class JournalRepository {
  static Future<void> init();
  
  // CRUD
  Future<void> saveEntry(JournalEntryHive entry);
  Future<void> updateEntry(JournalEntryHive entry);
  Future<void> deleteEntry(String entryId);
  JournalEntryHive? getEntryById(String entryId);
  
  // 查询
  List<JournalEntryHive> getAllEntries();
  List<JournalEntryHive> getEntriesByType(String type);
  List<JournalEntryHive> getEntriesByDate(DateTime date);
  List<JournalEntryHive> getEntriesByTag(String tag);
  List<JournalEntryHive> getPinnedEntries();
  
  // 搜索
  List<JournalEntryHive> searchEntries(String query);
  
  // 统计
  int getEntryCountByType(String type);
  List<String> getAllTags();
  
  // 业务
  Future<void> togglePin(String entryId);
  Future<void> addTag(String entryId, String tag);
  Future<void> removeTag(String entryId, String tag);
}
```

---

## 4. Provider 层

### JournalProvider

```dart
class JournalProvider extends ChangeNotifier {
  final JournalRepository _repository;
  
  // 状态
  List<JournalEntryHive> _entries = [];
  String _selectedType = 'all';              // all/diary/weekly/note
  String? _selectedTag;
  String _searchQuery = '';
  
  // Getters
  List<JournalEntryHive> get entries;        // 当前筛选的记录列表
  List<JournalEntryHive> get pinnedEntries;  // 置顶记录
  List<String> get allTags;                  // 所有标签
  Map<String, int> get entryCountByType;     // 各类型数量统计
  
  // 核心方法
  Future<void> createEntry({
    required String type,
    String? title,
    String content = '',
    List<String> tags = const [],
  });
  
  Future<void> updateEntry(JournalEntryHive entry);
  Future<void> deleteEntry(String entryId);
  
  // 筛选
  void selectType(String type);
  void selectTag(String? tag);
  void setSearchQuery(String query);
  
  // 自动关联
  Future<void> autoFillDailyData(JournalEntryHive entry);  // 填充专注数据
  
  // 标签管理
  Future<void> addTagToEntry(String entryId, String tag);
  Future<void> removeTagFromEntry(String entryId, String tag);
  
  // 置顶
  Future<void> togglePin(String entryId);
}
```

---

## 5. UI 设计

### 5.1 记录主页面（JournalScreen）

**布局结构**（两栏）：
```
┌────────────┬──────────────────────────────┐
│            │  📝 记录          [+ 新建]   │
│  筛选栏    ├──────────────────────────────┤
│  (200px)   │                              │
│            │  [置顶] 学习Flutter心得      │
│  全部      │  📌 2025-10-08               │
│  日记      │  今天完成了专注功能...        │
│  周记      │  🏷️ #学习 #Flutter           │
│  随笔      │                              │
│            │  ─────────────────────────   │
│  ─────     │                              │
│            │  今日总结                     │
│  标签      │  📅 2025-10-08               │
│  #学习     │  完成了4个番茄钟...           │
│  #工作     │  🍅 100分钟                  │
│  #灵感     │                              │
│            │  ─────────────────────────   │
└────────────┴──────────────────────────────┘
```

**组件**：
- `JournalFilterSidebar` - 左侧筛选栏
- `JournalEntryCard` - 记录卡片（列表项）
- `EmptyState` - 空状态提示

### 5.2 编辑器对话框（JournalEditorDialog）

**布局**：
```
┌────────────────────────────────────────┐
│  新建日记                    [编辑/预览] │
├────────────────────────────────────────┤
│  标题（可选）                          │
│  [_____________________________]       │
│                                        │
│  [B] [I] [•] [H] [`]  ← 简单工具栏   │
│  ──────────────────────────────────   │
│                                        │
│  # 编辑区域                            │
│  支持 Markdown 语法...                 │
│  **粗体** *斜体*                       │
│                                        │
│  ──────────────────────────────────   │
│  🏷️ 标签： [+添加标签]                │
│  [#学习] [#工作]                       │
│                                        │
│  😊 心情： [😊] [😐] [😔] [😫]        │
│                                        │
│           [取消]  [保存]               │
└────────────────────────────────────────┘
```

**组件**：
- `MarkdownEditor` - Markdown 编辑器组件
- `MarkdownToolbar` - 格式化工具栏
- `TagSelector` - 标签选择器
- `MoodSelector` - 心情选择器

---

## 6. 核心组件设计

### 6.1 MarkdownEditor 组件

```dart
class MarkdownEditor extends StatefulWidget {
  final TextEditingController controller;
  final bool isPreviewMode;
  final VoidCallback? onTogglePreview;
  
  @override
  State<MarkdownEditor> createState() => _MarkdownEditorState();
}

class _MarkdownEditorState extends State<MarkdownEditor> {
  Widget build(BuildContext context) {
    return Column(
      children: [
        MarkdownToolbar(controller: controller),
        Expanded(
          child: widget.isPreviewMode
              ? _buildPreview()
              : _buildEditor(),
        ),
      ],
    );
  }
  
  Widget _buildEditor();    // TextField 编辑
  Widget _buildPreview();   // Markdown 渲染
}
```

### 6.2 MarkdownToolbar 工具栏

```dart
class MarkdownToolbar extends StatelessWidget {
  final TextEditingController controller;
  
  Widget build(BuildContext context) {
    return Row(
      children: [
        _buildButton(Icons.format_bold, '粗体', _insertBold),
        _buildButton(Icons.format_italic, '斜体', _insertItalic),
        _buildButton(Icons.format_list_bulleted, '列表', _insertList),
        _buildButton(Icons.title, '标题', _insertHeading),
        _buildButton(Icons.code, '代码', _insertCode),
      ],
    );
  }
  
  void _insertBold();       // 插入 **text**
  void _insertItalic();     // 插入 *text*
  void _insertList();       // 插入 - 
  void _insertHeading();    // 插入 ## 
  void _insertCode();       // 插入 `code`
}
```

---

## 7. 与现有功能集成

### 7.1 专注完成集成

**在专注完成对话框添加**：
```dart
// FocusScreen 完成对话框
actions: [
  TextButton('稍后'),
  TextButton.icon(
    icon: Icon(Icons.edit_note),
    label: '写点感想',
    onPressed: _createJournalFromFocus,  // 快捷创建记录
  ),
  FilledButton.icon('开始休息'),
]
```

**自动填充内容**：
```markdown
# 专注记录 - 2025-10-08 14:30

刚刚完成了「学习Flutter」任务的专注会话

**专注时长**：25分钟
**连续番茄钟**：第3个

## 收获和感想
[用户填写...]
```

### 7.2 首页集成

**HomeScreen 添加快捷入口**：
```dart
Card(
  title: '📝 今日记录',
  child: InkWell(
    onTap: _openTodayJournal,
    child: Text('点击添加今日总结...'),
  ),
)
```

### 7.3 日历集成

**日历视图标记**：
- 有记录的日期显示小圆点
- 点击日期查看/创建当日记录

---

## 8. 📋 实施行动清单

### 步骤1：数据层搭建（1天）

#### 待办事项
- [ ] 创建 `journal_entry_hive.dart` 数据模型
- [ ] 定义所有 @HiveField (0-10)
- [ ] 实现计算属性：`displayTitle`, `dateFormatted`, `wordCount`
- [ ] 实现 `copyWith()` 方法
- [ ] 运行 `build_runner` 生成适配器
- [ ] 创建 `JournalRepository`
- [ ] 实现 CRUD 方法和查询方法
- [ ] 在 `main.dart` 注册适配器和初始化

#### ✅ 验收检查
- [ ] 无编译错误，适配器生成成功
- [ ] 可以创建并保存记录到数据库
- [ ] 可以按类型、日期、标签查询
- [ ] 搜索功能正常
- [ ] 应用重启后数据持久化

---

### 步骤2：业务逻辑层（1天）

#### 待办事项
- [ ] 创建 `JournalProvider`
- [ ] 添加状态属性：`_entries`, `_selectedType`, `_selectedTag`, `_searchQuery`
- [ ] 实现 Getters：`entries`, `pinnedEntries`, `allTags`, `entryCountByType`
- [ ] 实现核心方法：`createEntry()`, `updateEntry()`, `deleteEntry()`
- [ ] 实现筛选方法：`selectType()`, `selectTag()`, `setSearchQuery()`
- [ ] 实现 `autoFillDailyData()` - 自动填充专注数据
- [ ] 实现标签管理：`addTagToEntry()`, `removeTagFromEntry()`
- [ ] 实现置顶：`togglePin()`
- [ ] 在 `main.dart` 注册 Provider

#### ✅ 验收检查
- [ ] 创建/编辑/删除记录正常
- [ ] 类型筛选正常
- [ ] 标签筛选正常
- [ ] 搜索功能正常
- [ ] 置顶功能正常
- [ ] 自动填充专注数据正确

---

### 步骤3：UI层 - Markdown编辑器（1.5天）

#### 待办事项
- [ ] 创建 `MarkdownEditor` 组件
  - [ ] 编辑模式：TextField 多行输入
  - [ ] 预览模式：使用 `flutter_markdown` 渲染
  - [ ] 切换按钮：[编辑] / [预览]
- [ ] 创建 `MarkdownToolbar` 工具栏组件
  - [ ] 粗体按钮 - 插入 `**text**`
  - [ ] 斜体按钮 - 插入 `*text*`
  - [ ] 列表按钮 - 插入 `- `
  - [ ] 标题按钮 - 插入 `## `
  - [ ] 代码按钮 - 插入 `` `code` ``
  - [ ] 实现 `_wrapSelection()` - 包裹选中文本
  - [ ] 实现 `_insertAtLineStart()` - 行首插入
- [ ] 创建 `TagSelector` 标签选择器
  - [ ] 显示已有标签
  - [ ] 添加新标签
  - [ ] 删除标签
- [ ] 创建 `MoodSelector` 心情选择器
  - [ ] 5个emoji选项：😊 😐 😔 😫 😴
- [ ] 创建 `JournalEditorDialog` 编辑对话框
  - [ ] 整合所有组件
  - [ ] 标题输入框
  - [ ] Markdown编辑器
  - [ ] 标签选择器
  - [ ] 心情选择器
  - [ ] 保存逻辑

#### ✅ 验收检查
- [ ] 编辑器可以输入文本
- [ ] 工具栏按钮插入正确的 Markdown 语法
- [ ] 预览模式正确渲染 Markdown
- [ ] 标签可以添加/删除
- [ ] 心情可以选择
- [ ] 保存后数据正确

---

### 步骤4：UI层 - 主页面和列表（1.5天）

#### 待办事项
- [ ] 创建 `JournalScreen` 主页面
  - [ ] 两栏布局（筛选栏 | 记录列表）
  - [ ] 顶部操作栏（标题 + 新建按钮）
- [ ] 创建 `JournalFilterSidebar` 筛选栏
  - [ ] 类型筛选：全部、日记、周记、随笔
  - [ ] 标签列表（动态生成）
  - [ ] 显示各类型数量
- [ ] 创建 `JournalEntryCard` 记录卡片
  - [ ] 显示标题、日期、摘要
  - [ ] 显示标签、心情
  - [ ] 显示专注时长（如果有）
  - [ ] 置顶标记
  - [ ] 操作按钮（编辑、删除、置顶）
- [ ] 创建空状态组件
- [ ] 在主导航添加"记录"入口

#### ✅ 验收检查
- [ ] 进入记录页面，布局正确
- [ ] 点击"新建"，打开编辑对话框
- [ ] 保存后列表显示新记录
- [ ] 类型筛选正常切换
- [ ] 标签筛选正常
- [ ] 置顶功能正常
- [ ] 编辑/删除功能正常

---

### 步骤5：集成和优化（1天）

#### 待办事项
- [ ] **专注完成集成**
  - [ ] 在 `FocusScreen` 完成对话框添加"写点感想"按钮
  - [ ] 自动创建记录并填充专注数据
- [ ] **首页集成**
  - [ ] 在 `HomeScreen` 添加今日记录快捷入口
  - [ ] 显示最近的一条记录摘要
- [ ] **日历集成**（可选）
  - [ ] 在日历视图标记有记录的日期
  - [ ] 点击日期快速创建/查看记录
- [ ] **搜索功能**
  - [ ] 添加搜索框
  - [ ] 实现全文搜索
  - [ ] 高亮搜索关键词
- [ ] **导出功能**（可选）
  - [ ] 导出单条记录为 Markdown 文件
  - [ ] 导出选定日期范围的记录
- [ ] 全面测试

#### ✅ 验收检查

**功能测试**
- [ ] 创建日记/周记/随笔都正常
- [ ] Markdown 语法渲染正确
- [ ] 标签系统正常
- [ ] 搜索功能正常
- [ ] 置顶功能正常

**集成测试**
- [ ] 专注完成后可快速创建记录
- [ ] 自动填充专注数据正确
- [ ] 首页快捷入口正常
- [ ] 从任务详情可创建相关笔记

**性能测试**
- [ ] 加载100+条记录流畅
- [ ] 搜索响应迅速
- [ ] Markdown 渲染流畅

---

## 9. Markdown 技术方案

### 9.1 依赖配置

```yaml
dependencies:
  flutter_markdown: ^0.7.0    # 官方 Markdown 渲染
  markdown: ^7.0.0             # Markdown 解析（自动依赖）
```

### 9.2 编辑器实现

**编辑模式**（TextField）：
```dart
TextField(
  controller: _contentController,
  maxLines: null,
  expands: true,
  decoration: InputDecoration(
    hintText: '支持 Markdown 语法\n# 标题\n**粗体** *斜体*\n- 列表\n`代码`',
    border: InputBorder.none,
  ),
  style: TextStyle(fontFamily: 'monospace'),  // 等宽字体
)
```

**预览模式**（flutter_markdown）：
```dart
Markdown(
  data: _contentController.text,
  styleSheet: MarkdownStyleSheet.fromTheme(Theme.of(context)).copyWith(
    // 自定义样式
    h1: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
    p: TextStyle(fontSize: 16),
    code: TextStyle(backgroundColor: Colors.grey[200]),
  ),
  selectable: true,  // 可选择文本
)
```

### 9.3 工具栏实现

**文本包裹逻辑**：
```dart
void _wrapSelection(String prefix, String suffix) {
  final selection = _controller.selection;
  final text = _controller.text;
  
  if (selection.isValid && !selection.isCollapsed) {
    // 有选中文本：包裹
    final selectedText = text.substring(selection.start, selection.end);
    final newText = text.replaceRange(
      selection.start,
      selection.end,
      '$prefix$selectedText$suffix',
    );
    _controller.value = TextEditingValue(
      text: newText,
      selection: TextSelection.collapsed(
        offset: selection.start + prefix.length + selectedText.length + suffix.length,
      ),
    );
  } else {
    // 无选中：插入模板
    _insertText('${prefix}文本$suffix');
  }
}
```

**行首插入逻辑**：
```dart
void _insertAtLineStart(String prefix) {
  final selection = _controller.selection;
  final text = _controller.text;
  
  // 找到当前行的起始位置
  int lineStart = selection.start;
  while (lineStart > 0 && text[lineStart - 1] != '\n') {
    lineStart--;
  }
  
  // 在行首插入
  final newText = text.substring(0, lineStart) + 
                  prefix + 
                  text.substring(lineStart);
  
  _controller.value = TextEditingValue(
    text: newText,
    selection: TextSelection.collapsed(
      offset: lineStart + prefix.length,
    ),
  );
}
```

---

## 10. 快捷功能

### 10.1 快速创建记录

**从专注完成**：
```dart
Future<void> createJournalFromFocus(FocusSessionHive session) async {
  final task = session.taskId != null 
      ? _taskRepo.getTaskById(session.taskId!) 
      : null;
  
  final content = '''
# 专注记录 - ${DateFormat('HH:mm').format(session.endTime)}

刚刚完成了${task != null ? '「${task.title}」任务的' : ''}专注会话

**专注时长**：${session.actualDuration ~/ 60}分钟
**连续番茄钟**：第${_consecutivePomodoroCount}个

## 收获和感想
''';

  await createEntry(
    type: 'note',
    title: '专注记录 - ${task?.title ?? ''}',
    content: content,
    tags: ['专注'],
    relatedTaskIds: task != null ? [task.id] : [],
  );
}
```

### 10.2 模板系统

**日记模板**：
```markdown
# YYYY-MM-DD 日记

## 今日完成
- 

## 收获和感悟
- 

## 明天计划
- 
```

**周记模板**：
```markdown
# 第X周周记 (MM.DD - MM.DD)

## 本周成就
- 

## 遇到的挑战
- 

## 下周重点
- 
```

---

## 11. 注意事项

### 技术注意事项
- ⚠️ Markdown 解析性能：长文本（>10000字）需要优化
- ⚠️ 数据存储：每条记录建议限制在 50KB 以内
- ⚠️ 自动保存：使用防抖（debounce），避免频繁写入

### 用户体验注意事项
- ✅ 编辑时自动保存草稿（防丢失）
- ✅ 删除前二次确认
- ✅ 支持快捷键（Ctrl+B、Ctrl+I等）
- ✅ 预览模式支持滚动同步

---

## 12. 扩展功能（后续版本）

### v2.0
- 📷 图片支持（Markdown 语法 `![](url)`）
- 📊 图表支持（mermaid 语法）
- 🔗 双向链接（类似 Obsidian）
- 📤 导出 PDF

### v3.0
- 🤖 AI 分析记录（情绪分析、效率洞察）
- 📈 记录统计（字数、频率趋势）
- 🌐 云端同步
- 👥 分享功能

---

## 13. 验收标准

### 功能完整性
- [x] 支持 Markdown 编辑和渲染
- [x] 三种记录类型完整
- [x] 标签系统完整
- [x] 搜索功能完整
- [x] 与专注功能集成

### 数据准确性
- [x] 记录保存正确
- [x] 自动关联数据准确
- [x] 搜索结果准确

### 用户体验
- [x] 编辑流畅
- [x] 预览渲染正确
- [x] 操作直观
- [x] 错误提示友好

---

## 14. 技术栈

```yaml
# 必须依赖
flutter_markdown: ^0.7.0        # Markdown 渲染

# 可选依赖（如果需要代码高亮）
flutter_highlight: ^0.7.0       # 代码语法高亮
highlight: ^0.7.0               # 高亮库

# 其他工具
intl: ^0.19.0                   # 日期格式化（已有）
```

---

## 15. 文件结构

```
lib/data/models/
└── journal_entry_hive.dart              ✅ TypeId: 7

lib/data/repositories/
└── journal_repository.dart              ✅ 记录仓库

lib/presentation/providers/
└── journal_provider.dart                ✅ 状态管理

lib/presentation/screens/journal/
├── journal_screen.dart                  ✅ 主页面
├── widgets/
│   ├── journal_filter_sidebar.dart     ✅ 筛选栏
│   ├── journal_entry_card.dart         ✅ 记录卡片
│   ├── markdown_editor.dart            ✅ Markdown 编辑器
│   ├── markdown_toolbar.dart           ✅ 格式工具栏
│   ├── tag_selector.dart               ✅ 标签选择器
│   └── mood_selector.dart              ✅ 心情选择器
└── dialogs/
    └── journal_editor_dialog.dart       ✅ 编辑对话框

lib/main.dart                            ✅ 注册适配器和Provider
```

---

## 16. 快速命令

```bash
# 添加依赖
flutter pub add flutter_markdown

# 生成适配器
flutter packages pub run build_runner build --delete-conflicting-outputs

# TypeId 7 - JournalEntryHive
```

---

**文档版本**：v1.0  
**创建日期**：2025年10月8日  
**预计实施**：4-5天  
**TypeId 使用**：7（JournalEntryHive）  
**状态**：待实施 📝

