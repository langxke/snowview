# 工具执行结果展示功能实施方案

## 📋 概述

**目标**：让 UI 组件能够访问工具执行结果，正确展开批量操作的详细信息

**方案**：在 `ChatMessageHive` 中添加 `toolResults` 字段，存储工具执行结果

**影响范围**：
- 数据模型层（1个文件）
- 业务逻辑层（1个文件）
- UI层（2个文件）

**预计工作量**：1-2小时

---

## 🎯 实施步骤

### Step 1: 数据模型扩展（15分钟）

#### 1.1 修改 ChatMessageHive 数据模型

**文件**：`lib/data/models/chat_message_hive.dart`

**修改内容**：

```dart
@HiveType(typeId: 10)
class ChatMessageHive extends HiveObject {
  @HiveField(0)
  String id;
  
  @HiveField(1)
  String role;
  
  @HiveField(2)
  String content;
  
  @HiveField(3)
  DateTime timestamp;
  
  @HiveField(4)
  String? toolCallId;
  
  @HiveField(5)
  List<String>? toolCalls;
  
  @HiveField(6)
  String? sessionId;
  
  // ✅ 新增字段
  /// 工具执行结果（JSON字符串）
  /// 格式：Map<toolCallId, result>
  /// 示例：{"call_123": {"success": true, "deletedSubtasks": ["步骤1", "步骤2"]}}
  @HiveField(7)
  String? toolResults;
  
  ChatMessageHive({
    required this.id,
    required this.role,
    required this.content,
    required this.timestamp,
    this.toolCallId,
    this.toolCalls,
    this.sessionId,
    this.toolResults,  // ✅ 添加到构造函数
  });
  
  // ✅ 修改 assistant 工厂方法
  factory ChatMessageHive.assistant({
    required String id,
    required String content,
    required String sessionId,
    DateTime? timestamp,
    List<String>? toolCalls,
    String? toolResults,  // ✅ 新增参数
  }) {
    return ChatMessageHive(
      id: id,
      role: 'assistant',
      content: content,
      timestamp: timestamp ?? DateTime.now(),
      toolCalls: toolCalls,
      toolResults: toolResults,  // ✅ 传递参数
      sessionId: sessionId,
    );
  }
  
  // ✅ 修改 copyWith 方法
  ChatMessageHive copyWith({
    String? id,
    String? role,
    String? content,
    DateTime? timestamp,
    Object? toolCallId = _undefined,
    Object? toolCalls = _undefined,
    String? sessionId,
    Object? toolResults = _undefined,  // ✅ 新增参数
  }) {
    return ChatMessageHive(
      id: id ?? this.id,
      role: role ?? this.role,
      content: content ?? this.content,
      timestamp: timestamp ?? this.timestamp,
      toolCallId: toolCallId == _undefined ? this.toolCallId : toolCallId as String?,
      toolCalls: toolCalls == _undefined ? this.toolCalls : toolCalls as List<String>?,
      sessionId: sessionId ?? this.sessionId,
      toolResults: toolResults == _undefined ? this.toolResults : toolResults as String?,  // ✅ 新增逻辑
    );
  }
}
```

#### 1.2 重新生成 Hive 适配器

**命令**：
```bash
cd snowview
flutter pub run build_runner build --delete-conflicting-outputs
```

**验证**：检查 `chat_message_hive.g.dart` 是否包含 `toolResults` 字段的读写逻辑

---

### Step 2: 工具编排器更新（30分钟）

#### 2.1 修改创建执行计划消息

**文件**：`lib/presentation/providers/ai_tool_call_orchestrator.dart`

**位置**：`_createExecutionPlanMessage` 方法

**修改**：
```dart
ChatMessageHive _createExecutionPlanMessage(
  List<OpenAIToolCall> toolCalls,
  String sessionId,
) {
  final toolCallsJson = toolCalls.map((tc) {
    return jsonEncode({
      'id': tc.id,
      'type': tc.type,
      'function': {
        'name': tc.function.name,
        'arguments': _parseToolArguments(tc.function.arguments),
      },
    });
  }).toList();
  
  return ChatMessageHive.assistant(
    id: const Uuid().v4(),
    content: '',
    toolCalls: toolCallsJson,
    toolResults: null,  // ✅ 初始为 null，执行后更新
    sessionId: sessionId,
  );
}
```

#### 2.2 修改工具执行流程 - 收集并存储结果

**文件**：`lib/presentation/providers/ai_tool_call_orchestrator.dart`

**位置**：`_executeAllTools` 方法

**修改**：
```dart
Future<List<ToolExecutionResult>> _executeAllTools(
  List<OpenAIToolCall> toolCalls,
  String sessionId,
  Future<void> Function(ChatMessageHive) onMessageGenerated,
) async {
  final results = <ToolExecutionResult>[];
  
  // 打印执行计划（保持不变）
  if (toolCalls.length > 1) {
    print('[Tool Orchestrator] 📋 执行计划（顺序执行）：');
    for (int i = 0; i < toolCalls.length; i++) {
      print('[Tool Orchestrator]   ${i + 1}. ${toolCalls[i].function.name}');
    }
  }
  
  // ✅ 1. 生成执行计划消息（但先不保存）
  ChatMessageHive? planMessage;
  
  for (int i = 0; i < toolCalls.length; i++) {
    if (shouldStop) {
      print('[Tool Orchestrator] 🛑 执行过程中被停止（已完成$i/${toolCalls.length}）');
      break;
    }
    
    final toolCall = toolCalls[i];
    
    // ✅ 2. 首次循环时创建并保存计划消息
    if (i == 0) {
      planMessage = _createExecutionPlanMessage(toolCalls, sessionId);
      await onMessageGenerated(planMessage);
    }
    
    // 3. 执行工具（保持不变）
    final result = await _executeSingleTool(toolCall);
    results.add(result);
    
    // 4. 生成工具结果消息（保持不变）
    final toolMessage = ChatMessageHive.tool(
      id: const Uuid().v4(),
      content: jsonEncode(result.toJson()),
      toolCallId: toolCall.id,
      sessionId: sessionId,
    );
    await onMessageGenerated(toolMessage);
    
    if (toolCalls.length > 1) {
      print('[Tool Orchestrator] ✅ [${i + 1}/${toolCalls.length}] ${toolCall.function.name}');
    }
  }
  
  // ✅ 5. 执行完成后，更新计划消息的 toolResults
  if (planMessage != null && results.isNotEmpty) {
    final resultsMap = <String, dynamic>{};
    for (int i = 0; i < results.length; i++) {
      resultsMap[toolCalls[i].id] = results[i].result ?? {};
    }
    
    // 更新 planMessage
    planMessage.toolResults = jsonEncode(resultsMap);
    
    // 保存更新后的消息（利用 HiveObject 的 save 方法）
    await planMessage.save();
    
    print('[Tool Orchestrator] 💾 已保存工具执行结果到消息');
  }
  
  // 打印执行总结（保持不变）
  if (toolCalls.length > 1) {
    final businessSuccessCount = results.where((r) => 
      r.result != null && r.result!['success'] == true
    ).length;
    final businessFailureCount = results.where((r) => 
      r.result != null && r.result!['success'] == false
    ).length;
    final exceptionCount = results.where((r) => !r.success).length;
    
    print('[Tool Orchestrator] 📊 执行总结：业务成功 $businessSuccessCount 个，业务失败 $businessFailureCount 个，调用异常 $exceptionCount 个');
  }
  
  return results;
}
```

---

### Step 3: UI 组件适配（20分钟）

#### 3.1 修改消息气泡组件 - 传递 toolResults

**文件**：`lib/presentation/widgets/common/chat_message_bubble.dart`

**位置**：AIToolCallWidget 调用处

**修改**：
```dart
// 在 build 方法中，找到 AIToolCallWidget 的调用位置
if (message.toolCalls != null && message.toolCalls!.isNotEmpty)
  Padding(
    padding: EdgeInsets.only(
      top: message.content.trim().isNotEmpty ? 4 : 0,
    ),
    child: AIToolCallWidget(
      toolCallsJson: message.toolCalls!,
      toolResults: message.toolResults,  // ✅ 传递工具执行结果
    ),
  ),
```

#### 3.2 修改工具调用展示组件 - 接收并使用 toolResults

**文件**：`lib/presentation/widgets/common/ai_tool_call_widget.dart`

**修改 1**：添加 toolResults 参数
```dart
class AIToolCallWidget extends StatefulWidget {
  final List<String> toolCallsJson;
  final String? toolResults;  // ✅ 新增参数
  
  const AIToolCallWidget({
    super.key,
    required this.toolCallsJson,
    this.toolResults,  // ✅ 可选参数
  });
  
  @override
  State<AIToolCallWidget> createState() => _AIToolCallWidgetState();
}
```

**修改 2**：添加结果解析方法
```dart
class _AIToolCallWidgetState extends State<AIToolCallWidget> {
  bool _isExpanded = false;
  
  /// ✅ 解析工具执行结果
  Map<String, dynamic> _getToolResults() {
    if (widget.toolResults == null || widget.toolResults!.isEmpty) {
      return {};
    }
    try {
      return jsonDecode(widget.toolResults!) as Map<String, dynamic>;
    } catch (e) {
      print('[AIToolCallWidget] ⚠️ 解析 toolResults 失败: $e');
      return {};
    }
  }
  
  // ... 其他方法保持不变 ...
}
```

**修改 3**：在批量操作展开时使用结果
```dart
/// 构建批量操作的详细列表
List<Widget> _buildBatchOperationDetails(
  BuildContext context,
  Map<String, dynamic> toolCall,
  ColorScheme colorScheme,
  Color mutedColor,
) {
  final widgets = <Widget>[];
  
  try {
    final functionName = toolCall['function']?['name'] ?? '';
    final arguments = toolCall['function']?['arguments'];
    final toolCallId = toolCall['id'] as String?;  // ✅ 获取 toolCallId
    
    Map<String, dynamic> args;
    if (arguments is Map) {
      args = Map<String, dynamic>.from(arguments);
    } else if (arguments is String) {
      args = jsonDecode(arguments) as Map<String, dynamic>;
    } else {
      return [const SizedBox.shrink()];
    }
    
    // ✅ 获取该工具的执行结果
    final allResults = _getToolResults();
    final result = (toolCallId != null && allResults.containsKey(toolCallId))
        ? allResults[toolCallId] as Map<String, dynamic>?
        : null;
    
    // 批量创建任务（保持不变）
    if (functionName == 'batch_create_tasks' && args.containsKey('tasks')) {
      final tasks = args['tasks'] as List;
      for (var i = 0; i < tasks.length; i++) {
        final task = tasks[i] as Map<String, dynamic>;
        final title = task['title'] ?? '未命名任务';
        final date = task['date'] != null ? ' · ${task['date']}' : '';
        widgets.add(_buildBatchItem(colorScheme, mutedColor, title + date));
      }
    }
    
    // 批量删除任务（保持不变）
    else if (functionName == 'batch_delete_tasks' && args.containsKey('taskIds')) {
      final taskIds = args['taskIds'] as List;
      final taskRepository = context.read<TaskListRepository>();
      
      for (var i = 0; i < taskIds.length; i++) {
        final taskId = taskIds[i].toString();
        final task = taskRepository.getTaskById(taskId);
        final displayText = task != null ? task.title : '任务 ${i + 1}';
        widgets.add(_buildBatchItem(colorScheme, mutedColor, displayText));
      }
    }
    
    // 批量创建日程（保持不变）
    else if (functionName == 'batch_create_calendar_events' && args.containsKey('events')) {
      final events = args['events'] as List;
      for (var i = 0; i < events.length; i++) {
        final event = events[i] as Map<String, dynamic>;
        final title = event['title'] ?? '未命名日程';
        final date = event['date'] != null ? ' · ${event['date']}' : '';
        final time = event['startTime'] != null ? ' ${event['startTime']}' : '';
        widgets.add(_buildBatchItem(colorScheme, mutedColor, title + date + time));
      }
    }
    
    // 批量删除日程（保持不变）
    else if (functionName == 'batch_delete_calendar_events' && args.containsKey('eventIds')) {
      final eventIds = args['eventIds'] as List;
      final eventBox = Hive.box<CalendarEventHive>('calendar_events');
      
      for (var i = 0; i < eventIds.length; i++) {
        final eventId = eventIds[i].toString();
        final event = eventBox.get(eventId);
        if (event != null) {
          final dateStr = event.start.toString().split(' ')[0];
          widgets.add(_buildBatchItem(colorScheme, mutedColor, '${event.title} ($dateStr)'));
        } else {
          widgets.add(_buildBatchItem(colorScheme, mutedColor, '日程 ${i + 1}'));
        }
      }
    }
    
    // 批量添加子步骤（保持不变）
    else if (functionName == 'batch_add_subtasks' && args.containsKey('subtasks')) {
      final subtasks = args['subtasks'] as List;
      for (var i = 0; i < subtasks.length; i++) {
        final subtaskTitle = subtasks[i].toString();
        widgets.add(_buildBatchItem(colorScheme, mutedColor, subtaskTitle));
      }
    }
    
    // ✅ 批量删除子步骤 - 优先使用执行结果
    else if (functionName == 'batch_delete_subtasks' && args.containsKey('subtaskIds')) {
      // ✅ 优先使用执行结果中的已删除子步骤列表
      if (result != null && result['deletedSubtasks'] != null) {
        final deletedSubtasks = result['deletedSubtasks'] as List;
        for (final title in deletedSubtasks) {
          widgets.add(_buildBatchItem(colorScheme, mutedColor, title.toString()));
        }
      } 
      // 降级方案1：尝试从当前任务中查找（如果子步骤还未删除）
      else {
        final subtaskIds = args['subtaskIds'] as List;
        final taskId = args['taskId'] as String?;
        
        if (taskId != null) {
          final taskRepository = context.read<TaskListRepository>();
          final task = taskRepository.getTaskById(taskId);
          
          if (task != null) {
            for (var i = 0; i < subtaskIds.length; i++) {
              final subtaskId = subtaskIds[i].toString();
              final subtask = task.subTasks.where((st) => st.id == subtaskId).firstOrNull;
              final displayText = subtask != null ? subtask.title : '子步骤 ${i + 1}';
              widgets.add(_buildBatchItem(colorScheme, mutedColor, displayText));
            }
          } else {
            // 降级方案2：显示序号
            for (var i = 0; i < subtaskIds.length; i++) {
              widgets.add(_buildBatchItem(colorScheme, mutedColor, '子步骤 ${i + 1}'));
            }
          }
        } else {
          // 降级方案3：没有 taskId 时显示序号
          final subtaskIds = args['subtaskIds'] as List;
          for (var i = 0; i < subtaskIds.length; i++) {
            widgets.add(_buildBatchItem(colorScheme, mutedColor, '子步骤 ${i + 1}'));
          }
        }
      }
    }
  } catch (e) {
    print('[AIToolCallWidget] ⚠️ 构建批量操作详情失败: $e');
    return [const SizedBox.shrink()];
  }
  
  return widgets.isEmpty ? [const SizedBox.shrink()] : widgets;
}
```

---

### Step 4: 测试验证（15分钟）

#### 4.1 编译验证

```bash
flutter pub run build_runner build --delete-conflicting-outputs
flutter analyze
```

**预期**：无编译错误，无 lint 警告

#### 4.2 功能测试

**测试用例 1：批量删除子步骤**
1. 创建一个任务并添加多个子步骤
2. 让 AI 删除所有子步骤
3. 点击展开按钮
4. **预期**：显示已删除的子步骤标题列表

**测试用例 2：批量创建任务**
1. 让 AI 批量创建 3 个任务
2. 点击展开按钮
3. **预期**：显示 3 个任务的标题

**测试用例 3：历史消息**
1. 查看之前的批量操作消息
2. 点击展开按钮
3. **预期**：正确显示详细列表（有结果则显示，无结果则降级显示）

**测试用例 4：降级处理**
1. 查看旧版本的消息（没有 toolResults）
2. 点击展开按钮
3. **预期**：显示序号或降级信息，不崩溃

#### 4.3 性能验证

- 查看消息加载时间
- 检查内存占用
- 验证数据库大小增长

**预期**：性能影响可忽略

---

## 📊 实施检查清单

### 代码修改
- [ ] `chat_message_hive.dart` - 添加 toolResults 字段
- [ ] 运行 `build_runner` 生成代码
- [ ] `ai_tool_call_orchestrator.dart` - 修改 _createExecutionPlanMessage
- [ ] `ai_tool_call_orchestrator.dart` - 修改 _executeAllTools
- [ ] `chat_message_bubble.dart` - 传递 toolResults
- [ ] `ai_tool_call_widget.dart` - 接收和使用 toolResults

### 测试验证
- [ ] 编译通过
- [ ] 批量删除子步骤展开正确
- [ ] 批量创建任务展开正确
- [ ] 历史消息兼容性测试
- [ ] 降级场景测试
- [ ] 性能测试

### 文档更新
- [ ] 更新数据模型文档
- [ ] 更新架构设计文档
- [ ] 记录实施结果

---

## ⚠️ 注意事项

### 1. 数据库兼容性
- ✅ 新字段默认为 `null`，旧数据自动兼容
- ✅ 无需手动迁移
- ⚠️ 不要删除 HiveField，只能添加

### 2. HiveObject.save() 方法
```dart
// HiveObject 继承自 HiveObjectMixin，提供 save() 方法
// 前提：对象必须已经在 box 中
await planMessage.save();  // ✅ 自动保存到数据库
```

### 3. 降级策略
UI 组件必须处理以下场景：
- toolResults 为 null（旧消息）
- toolResults 为空字符串
- toolResults 解析失败
- 执行结果中没有预期字段

所有情况都应降级显示，不能崩溃。

### 4. JSON 格式示例

**toolResults 格式**：
```json
{
  "call_abc123": {
    "success": true,
    "deletedSubtasks": ["子步骤1", "子步骤2", "子步骤3"],
    "totalCount": 3,
    "successCount": 3
  },
  "call_def456": {
    "success": true,
    "createdTasks": ["任务A", "任务B"]
  }
}
```

---

## 🚀 实施后续

### 短期优化
- 为所有批量操作添加结果展开支持
- 优化展开动画效果
- 添加结果统计显示

### 长期扩展
- 支持工具执行进度显示
- 支持工具执行重试
- 支持工具执行撤销

---

## 📝 实施记录

**开始时间**：____________________

**完成时间**：____________________

**实际耗时**：____________________

**遇到的问题**：
- 
- 

**解决方案**：
- 
- 

**实施人员**：____________________


