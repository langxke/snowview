## 要删除的目标
- 顶部“立即结束”的测试按钮，以及它为了强制结束时间块而写的整段测试逻辑。
- 左下角“手动触发任务清理”的测试按钮，以及与其绑定的调试入口方法。

## 已定位的位置
- “立即结束”测试按钮：
  - UI 位置：[schedule_screen.dart:L284-L333](file:///d:/Projects/SnowView/lib/presentation/screens/schedule_screen.dart#L284-L333)
  - 点击逻辑（修改 end=now 并触发确认弹窗）：[schedule_screen.dart:L380-L437](file:///d:/Projects/SnowView/lib/presentation/screens/schedule_screen.dart#L380-L437)
- “手动触发任务清理”测试按钮：
  - UI 位置：[main_screen.dart:L146-L160](file:///d:/Projects/SnowView/lib/presentation/screens/main_screen.dart#L146-L160)
  - 调试入口方法 `debugTriggerCleanup()`：[schedule_monitor.dart:L234-L242](file:///d:/Projects/SnowView/lib/presentation/widgets/schedule_monitor.dart#L234-L242)

## 实施步骤
1. 在 [schedule_screen.dart](file:///d:/Projects/SnowView/lib/presentation/screens/schedule_screen.dart) 删除“立即结束”按钮 widget；同时删除其 onPressed 的整段测试逻辑（包括查找 ScheduleMonitor、构造 ended event、updateScheduledEvent 等）。
2. 在 [main_screen.dart](file:///d:/Projects/SnowView/lib/presentation/screens/main_screen.dart) 删除左下角“手动触发任务清理”的 IconButton（含 tooltip/图标）。
3. 在 [schedule_monitor.dart](file:///d:/Projects/SnowView/lib/presentation/widgets/schedule_monitor.dart) 删除 `debugTriggerCleanup()` 方法；如果发现 `triggerCompletionForEvent(...)` 仅被“立即结束”按钮使用，也一并删除这个对外测试入口，避免留下无用 API。
4. 清理因删除导致的无用 import/变量。
5. 运行静态检查（Diagnostics/flutter analyze）确保无编译错误、无未引用符号。

## 不会改动的内容
- 自动清理过期任务的正式逻辑（`_performCleanupOnce()` / `_cleanupPastTasks()`）会保留，只删除手动触发入口。
- 正常的时间块结束监控与右下角确认弹窗逻辑不受影响。