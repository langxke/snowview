## 现状定位

* “清空安排”按钮目前在日程页顶部操作栏（按月/日/周视图分别显示），调用：

  * [schedule\_screen.dart:L284-L323](file:///d:/Projects/SnowView/lib/presentation/screens/schedule_screen.dart#L284-L323)

* 对应点击处理函数：

  * [schedule\_screen.dart:L555-L575](file:///d:/Projects/SnowView/lib/presentation/screens/schedule_screen.dart#L555-L575)

* 这些按钮最终调用：

  * [month\_ai\_scheduler.dart:runClearSchedule](file:///d:/Projects/SnowView/lib/presentation/screens/schedule/month_ai_scheduler.dart#L230-L273)

  * [day\_week\_ai\_scheduler.dart:runDayClearSchedule/runWeekClearSchedule](file:///d:/Projects/SnowView/lib/presentation/screens/schedule/day_week_ai_scheduler.dart#L358-L376)

* 仓库内目前只有 schedule\_screen.dart 在使用这些 clear 接口（删除按钮后，页面内的 3 个 handler 会变成未引用私有方法，需要一并删掉）。

## 删除目标

* 删除日程页顶部的“清空安排”按钮（在月/日/周三种视图分支里各 1 个）。

* 删除 `ScheduleScreen` 内 3 个仅为该按钮服务的 handler：`_onMonthClearSchedule/_onDayClearSchedule/_onWeekClearSchedule`。

## 实施步骤

1. 在 [schedule\_screen.dart](file:///d:/Projects/SnowView/lib/presentation/screens/schedule_screen.dart) 删除三个 `OutlinedButton.icon(label: Text('清空安排'))`。
2. 删除 3 个对应 handler 方法（`_onMonthClearSchedule/_onDayClearSchedule/_onWeekClearSchedule`）。
3. 清理由此产生的无用导入/引用（如果有）。
4. 运行静态检查（flutter analyze / Diagnostics）确认无未引用符号与编译错误。

## 不会改动的内容

* `MonthAiScheduler.runClearSchedule` 与 `DayWeekAiScheduler.runDayClearSchedule/runWeekClearSchedule` 的实现保留（只是 UI 不再暴露测试按钮入口）。

